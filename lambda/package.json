{
  "name": "leetvision-lambda",
  "version": "1.0.0",
  "description": "Lambda function for LeetVision AI code assistance",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}

